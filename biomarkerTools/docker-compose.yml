services:
  backend:
    build:
      context: .
      dockerfile: docker/backend.dockerfile
    ports:
      - "8160:8160"
    volumes:
      - ../tmp:/deploy/app/tmp
    environment:
      - PYTHONUNBUFFERED=1
